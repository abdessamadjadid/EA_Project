server:
  port: 8010
spring:
  application:
    name: EA Final Project (Dev)
  datasource:
    url: 'jdbc:postgresql://localhost:5432/cs544'
    username: 'root'
    password: 'CS544-EA'
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update